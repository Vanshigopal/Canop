import { ModuleStub } from "@/components/ModuleStub";

export function OmrPage() {
  return (
    <ModuleStub
      number={8}
      name="OMR Scanner"
      description="Scan answer sheets with the camera and auto-grade them."
      session={7}
    />
  );
}
