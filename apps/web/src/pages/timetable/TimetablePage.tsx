import { ModuleStub } from "@/components/ModuleStub";

export function TimetablePage() {
  return (
    <ModuleStub
      number={6}
      name="Timetable"
      description="Visual timetable builder for classes and exam schedules."
      session={6}
    />
  );
}
