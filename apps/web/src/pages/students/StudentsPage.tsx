import { ModuleStub } from "@/components/ModuleStub";

export function StudentsPage() {
  return (
    <ModuleStub
      number={2}
      name="Students"
      description="Student enrollment, profiles, batch assignment, and attendance history."
      session={5}
    />
  );
}
