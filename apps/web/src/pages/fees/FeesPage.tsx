import { ModuleStub } from "@/components/ModuleStub";

export function FeesPage() {
  return (
    <ModuleStub
      number={10}
      name="Fees"
      description="Fee structure setup, invoicing, and payment tracking."
      session={8}
    />
  );
}
