import { ModuleStub } from "@/components/ModuleStub";

export function EventsPage() {
  return (
    <ModuleStub
      number={20}
      name="Events"
      description="Event calendar for academic and extracurricular activities."
      session={10}
    />
  );
}
