import { ModuleStub } from "@/components/ModuleStub";

export function AttendancePage() {
  return (
    <ModuleStub
      number={5}
      name="Attendance"
      description="Mark and track daily attendance with QR codes and biometrics."
      session={6}
    />
  );
}
