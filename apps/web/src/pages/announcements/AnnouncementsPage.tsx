import { ModuleStub } from "@/components/ModuleStub";

export function AnnouncementsPage() {
  return (
    <ModuleStub
      number={15}
      name="Announcements"
      description="Broadcast announcements to students, teachers, and parents."
      session={10}
    />
  );
}
