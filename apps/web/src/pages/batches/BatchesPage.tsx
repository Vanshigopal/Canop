import { ModuleStub } from "@/components/ModuleStub";

export function BatchesPage() {
  return (
    <ModuleStub
      number={4}
      name="Batches"
      description="Create and manage student batches, assign teachers, and set schedules."
      session={5}
    />
  );
}
