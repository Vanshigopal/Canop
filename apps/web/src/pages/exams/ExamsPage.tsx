import { ModuleStub } from "@/components/ModuleStub";

export function ExamsPage() {
  return (
    <ModuleStub
      number={7}
      name="Exams & Marks"
      description="Create exams, enter marks, and generate result reports."
      session={7}
    />
  );
}
